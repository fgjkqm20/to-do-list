import { styled, Card } from '@mui/material';

/**
 * Todo Form Wrapper
 * @param {object} props - Component props
 * @returns {JSX.Element} Styled Card Component
 */
const TodoFormWrapper = styled(Card)`
  margin-top: 30px;
`;

export { TodoFormWrapper };
