import { grey } from '@mui/material/colors';

export const titleProps = {
  variant: 'h4',
  component: 'h1',
  sx: { color: grey[900] },
};

export const subTitleProps = {
  variant: 'h5',
  component: 'h2',
  sx: { color: grey[600] },
};
